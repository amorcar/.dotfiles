# TMUX configuration

set-option -g default-shell "/usr/local/bin/fish"

# set -g default-terminal "tmux-256color"
# set-option -g default-terminal screen-256color
set-option -sa terminal-overrides ',XXX:RGB'

################ Prefix ---------------------------------------------
# use C-s, since it's on the home row and easier to hit than C-b
set-option -g prefix C-s
unbind-key C-s
bind-key C-s send-prefix


################ General setup --------------------------------------
set -g base-index 1

# vi is good
setw -g mode-keys vi

# Activity Monitoring
setw -g monitor-activity on
set -g visual-activity on

set -g mouse on

setw -g aggressive-resize on

set -sg escape-time 0

set-option -g repeat-time 0

set-option -g set-titles off #on
set-option -g set-titles-string "#T - #W"

set-option -g allow-rename off

set-window-option -g display-panes-time 1500

set-option -g visual-activity off
set-option -g visual-bell off
set-option -g visual-silence off
set-window-option -g monitor-activity off
set-option -g bell-action none

################ Key bindings --------------------------------------
# Easy config reload
bind-key R source-file ~/.tmux.conf \; display-message "tmux.conf reloaded."

# Switch statusbar show
bind-key C-b set-option -g status

# Avoid delete window/pane confirmation
bind-key & kill-window
bind-key x kill-pane

# Resize pane shortcuts
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# synchronize all panes in a window
bind y setw synchronize-panes

bind-key : command-prompt
bind-key r refresh-client
# bind-key L clear-history

bind-key space next-window
bind-key bspace previous-window
bind-key enter next-layout

# use vim-like keys for splits and windows
unbind '"'
unbind %
bind-key v split-window -h
bind-key s split-window -v

# Vim keys to selec panes
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# Use Alt-vim keys without prefix key to switch panes
bind -n M-h select-pane -L
bind -n M-j select-pane -D
bind -n M-k select-pane -U
bind -n M-l select-pane -R

bind-key q display-panes

# Setup 'v' to begin selection as in Vim
bind P paste-buffer
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi y send-keys -X copy-selection
bind-key -T copy-mode-vi r send-keys -X rectangle-toggle

bind-key -n S-Left set-option -g status-position top
bind-key -n S-Right set-option -g status-position bottom


################ Panes lines colors -------------------------------
set -g pane-active-border-style bg=default,fg=green
set -g pane-border-style fg=default


# Clock
set-window-option -g clock-mode-colour green

################ Status line --------------------------------------
# set -g status off

set-option -g status-interval 1
set-option -g status-fg default

set -g status-right-length 100
# set -g status-left-length 100

set -g status-position bottom #bottom #top
set -g status-justify left

set-option -g status-style bg=default
set-option -g status-style fg=colour7

# Bars ---------------------------------
set -g status-right ""
set -g status-left ""

# set -g status-left "#[fg=brightgreen] \ue711 [#S] "
# set -g status-right "#[fg=brightgreen]#(curl icanhazip.com) \ue621 #(ifconfig en0 | grep 'inet ' | awk '{print \"\" $2}') #[fg=colour7] #(~/.config/scripts/battery.sh) \uf0e7 #[fg=colour7,bg=colour236] %d/%m #[fg=colour7,bg=colour236] %H:%M:%S"
# set -g status-right "#[fg=brightblack]#[bg=default]#[fg=colour7]#[bg=brightblack]#(~/.config/scripts/battery.sh) \uf0e7#[fg=brightblack]#[bg=default]#[fg=colour7] %d/%m #[fg=colour7] %H:%M:%S"
set -g status-right "#[fg=colour7]#[bg=default]#(~/.config/scripts/battery.sh) \uf0e7#[fg=colour7]  %d/%m #[fg=colour7] %H:%M:%S"

set -g status-justify left

# Windows ------------------------------
# set -g status-justify centre
# set -g window-status-format "#[fg=white,bg=default]#W"
# set -g window-status-current-format "#[fg=brightyellow,bg=default]#W"

# set -g window-status-current-format "#[fg=magenta]#[fg=black]#[bg=magenta]#I#[bg=brightblack]#[fg=white]#W#[fg=brightblack]#[bg=black]"
# set -g window-status-format "#[fg=yellow]#[fg=black]#[bg=yellow]#I#[bg=brightblack]#[fg=white]#W#[fg=brightblack]#[bg=black]"

# set -g window-status-current-format "#[fg=green]#[bg=brightblack]#[fg=white]#W#[fg=brightblack]#[bg=black]"
# set -g window-status-format "#[fg=brightblack]#[bg=black]#[bg=brightblack]#[fg=white]#W#[fg=brightblack]#[bg=black]"
set -g window-status-current-format "#[fg=green]#[bg=default][#[bg=default]#[fg=white]#W#[fg=green]#[bg=default]]"
set -g window-status-format "#[bg=default]#[fg=white]#W"
################ End config --------------------------------------
set -g default-terminal "xterm-256color"
