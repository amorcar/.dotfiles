#!/usr/bin/env python3

try:
    import requests
    from pprint import pprint as pp

    def get_info():
        url = "https://geo.myip.link/"
        return requests.get(url).json()

    info = get_info()
    pp(info)
        

except Exception:
    print("-")
